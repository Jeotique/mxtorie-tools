const {Collection} = require('./classes/Collection')
let {print, sleep, dateToEpoch, randomChar, randomNumber, isLink, toJSON} = require('./functions/utils')

module.exports = {
    Collection,
    print,
    sleep,
    dateToEpoch,
    randomChar,
    randomNumber,
    isLink, 
    toJSON
}
