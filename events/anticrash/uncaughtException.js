module.exports = {
    name: 'uncaughtException',

    run: async(client, reason, p)=>{
        return
        /*console.log(p)
        return console.log(`[ERREUR MINEUR] Erreur : \nRaison : ${reason}`)*/
    }
}