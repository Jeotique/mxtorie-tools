module.exports = {
    name: 'uncaughtExceptionMonitor',

    run: async(client, reason, p)=>{
        return
        /*console.log(p)
        return console.log(`[ERREUR] \nRaison : ${reason}`)*/
    }
}